// Default body font stack for overlays. Mirrors --font-body-default CSS var
// but needs a literal form for cases where the value gets serialized into
// user-overridable config (e.g. chatbox layout options saved in presets).
export const DEFAULT_OVERLAY_FONT_FAMILY =
  "'NanumSquare Neo', 'Pretendard Variable', Pretendard, -apple-system, 'Segoe UI', system-ui, sans-serif";
