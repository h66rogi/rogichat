"use client";

export function MarqueeOptions() {
  return (
    <span className="text-[11px] text-[var(--ps-text-muted)]">
      드래그로 영역을 그려 슬롯들을 한 번에 선택 (Shift = 추가, ⌥ = 빼기 — 곧 지원)
    </span>
  );
}
